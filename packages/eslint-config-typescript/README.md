# `@opd/eslint-config-typescript`

> eslint config used by `TypeScript` react app in pangu

## Usage

```bash
npm install @opd/eslint-config-typescript -D
```

```json
{
  "extends": ["@opd/eslint-config-typescript"]
}
```
